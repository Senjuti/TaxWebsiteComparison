The following Instructions assume that you have received all the 4 files, 3 java sources files and 1 text file named cleartax

1. Keep all the java source files in the same package, if you want to keep them in separate packages then you need to add the necessary import statements at the beginning
2. The main file is BenchmarTime.java give it a run to get the necessary output